<!doctype html>
<html>
    <head>
        {% include partials/headstuff %}
        <link rel="stylesheet" type="text/css" href="{{ pathtoroot }}_styles/chapters.css">
        <title>{{ title }} | {{ global.title }}</title>
    </head>
    <body>
        {{ content }}
    </body>
</html>