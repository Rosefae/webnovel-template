---
eleventyExcludeFromCollections: true
permalink: 404.html
---

<!doctype html>
<html lang="{{ global.language }}">
    <head>
        {% include partials/headstuff %}
        <title>404 | {{global.title}}</title>
    </head>
    <body class="page-404">
        {% include partials/darkmode %}
        <div class="page-wrapper">
            <div class="content-wrapper">
                {% include partials/header %}
                <div class="container">
                    <h1>404 &mdash; You lost?</h1>
                    <p class="no-indent">Looks like the page you were trying to go to doesn't exist. <a href="{{collections.index[0].url}}">Back to home?</a></p>
                </div>
                {% include partials/footer %}
            </div>
        </div>
        <script src="/_scripts/darkmode.js"></script>
    </body>
</html>